server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/job
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true


#logging.level:
  #org.hibernate.SQL: debug

